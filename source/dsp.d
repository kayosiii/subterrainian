module dsp;

import jack.c.types : jack_nframes_t;

/// this is the entry point for the jack processing thread 
/// to process the incomming audio data and then send it to
/// the outputs
void process( ) @nogc @safe nothrow
{

}